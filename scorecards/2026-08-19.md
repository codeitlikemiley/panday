# Scorecard — 2026-08-19

Generated by `cargo xtask scorecard` (docs/12 M12.4). Every number here comes from an
eval that also gates CI, so this report and the test suite cannot disagree.

## route-bench — task classification

```
  heuristic: accuracy 94% (47/50), confidently wrong 0, gated 3
    gated (wrong but not trusted): list failing tests (extract vs code), trap: test as a noun, trap: build a habit
```

Gate (docs/19): accuracy >= 70% and never confidently wrong — **pass**.

The trained classifier (M19.3) must beat this by >=10pt on the same corpus; `panday_router::shadow` is how a candidate is compared without letting it route.

## routing policy

- `default.yaml`: no unreachable rules.
- `dev.yaml`: no unreachable rules.
- `local.yaml`: no unreachable rules.

A finding here is a rule that can never fire, which means traffic is going somewhere other than where the file says. The fix is a YAML edit, and it is left to the human reading this PR — a generator that rewrote routing on its own would be the one component here making a decision nobody reviewed.

## reduce-bench — reduce-then-solve

Run by `cargo xtask reduce-bench` (M15.5) and by the nightly job; it needs an async runtime, so it is a separate task rather than a section here. Its gate — no recorded task may become unsolvable — blocks release on its own.

## what is not measured yet

- `agent-bench` (M19.6): 50 verifiable repo tasks. Needs T3 (M14.5).
- `json-bench` (M19.1): the suite exists (`panday_harness::json_bench`, 200 fixtures) and runs with `cargo xtask json-bench` against a gateway — but it needs a model, so no number appears here. CI has neither a GPU nor a GGUF, and a suite that skipped would put a green tick next to 'we did not measure'.
- Capability profiles (M18.4) ship **declared**, not measured; M19.2 measures them, and until then every profile says `(estimated)` in the system prompt.
