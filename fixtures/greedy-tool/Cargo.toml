# See ../demo-tool/Cargo.toml — same arrangement, wider world.
[workspace]

[package]
name = "greedy-tool"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.51"

[profile.release]
opt-level = "s"
strip = true
