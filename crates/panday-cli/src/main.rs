//! panday — the CLI. One codebase, two faces (ADR-012):
//!   `panday`      the ratatui TUI client
//!   `panday acp`  an ACP server over stdio → 13 editors become surfaces
//! First wiring: phase-0 `panday chat` streaming through the gateway.

fn main() {
    println!("panday: see docs/23-roadmap.md phase 0 — `panday chat` is the first exit criterion");
}
