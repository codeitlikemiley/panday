//! ferrum — the CLI. One codebase, two faces (ADR-012):
//!   `ferrum`      the ratatui TUI client
//!   `ferrum acp`  an ACP server over stdio → 13 editors become surfaces
//! First wiring: phase-0 `ferrum chat` streaming through the gateway.

fn main() {
    println!("ferrum: see docs/23-roadmap.md phase 0 — `ferrum chat` is the first exit criterion");
}
