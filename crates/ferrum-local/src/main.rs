//! `ferrum local` — one binary, zero egress (docs/18-local.md).
//! Composition target: harness + gateway-lite (local adapter only) +
//! llama-server/mistral.rs supervisor + SQLite event store. M18.1 first.

fn main() {
    println!("ferrum-local: see docs/18-local.md — first milestone M18.1");
}
